doctype html
html(lang="ru")
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
  body.body
    header.headr Главная
    main.main
      each user in users
        ul.user-list
          li
            p= `Пользователь с id: ${user.id}`
            p= `Названия поста ${user.post.title}`